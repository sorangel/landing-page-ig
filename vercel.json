{
  "framework": "vite",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/consulta-72h", "destination": "/consulta-72h.html" }
  ],
  "ignoreCommand": "git ls-files --others --exclude-standard --directory | wc -l | xargs test 0 -eq"
}
